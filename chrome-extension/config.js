// Central configuration for the Watch Party Chrome Extension
// This acts as the "environment variables" for the extension, since extensions don't natively support .env files.

export const CONFIG = {
    // The base URL of the live backend server
    BACKEND_URL: 'https://fluxparty-backend.onrender.com'
};
